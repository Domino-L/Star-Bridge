<Window x:Class="SCFleetCommand.Desktop.SquadEmblemCropWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Crop Squad Emblem"
        Width="560"
        Height="640"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Background="{StaticResource PageBackgroundBrush}"
          Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="48" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource HudPanel}">
            <Canvas x:Name="CropCanvas"
                    Background="{StaticResource FieldBackBrush}"
                    ClipToBounds="True"
                    MouseLeftButtonDown="CropCanvas_MouseLeftButtonDown"
                    MouseLeftButtonUp="CropCanvas_MouseLeftButtonUp"
                    MouseMove="CropCanvas_MouseMove"
                    SizeChanged="CropCanvas_SizeChanged">
                <Image x:Name="SourceImage"
                       Stretch="Uniform" />
                <Rectangle x:Name="ShadeTop" Fill="#99000000" />
                <Rectangle x:Name="ShadeLeft" Fill="#99000000" />
                <Rectangle x:Name="ShadeRight" Fill="#99000000" />
                <Rectangle x:Name="ShadeBottom" Fill="#99000000" />
                <Rectangle x:Name="CropFrame"
                           Width="220"
                           Height="220"
                           Stroke="{StaticResource AccentBrush}"
                           StrokeThickness="2"
                           Fill="#1100C8FF"
                           Cursor="SizeAll" />
            </Canvas>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="120" />
            </Grid.ColumnDefinitions>

            <TextBlock Text="Drag the square. Only the framed area becomes the squad emblem."
                       Foreground="{StaticResource MutedTextBrush}"
                       VerticalAlignment="Center" />
            <Button Grid.Column="1"
                    Margin="8,0,0,0"
                    Style="{StaticResource PrimaryButton}"
                    Content="Use Crop"
                    Click="UseCrop_Click" />
            <Button Grid.Column="2"
                    Margin="8,0,0,0"
                    Style="{StaticResource SecondaryButton}"
                    Content="Cancel"
                    Click="Cancel_Click" />
        </Grid>
    </Grid>
</Window>
